// Some application properties can't be included under source control (eg. DB passwords). This file serves to
//   2. document properties expected for the real "app-secret.config.js"
//   1. remind users that an "app-secret.config.js" is required

module.exports = {};
